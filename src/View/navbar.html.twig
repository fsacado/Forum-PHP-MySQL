<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="/?route=home">Forum Lolo</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="/?route=home">Menu principal <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">Link</a>
            </li>
            
            <li class="nav-item">
                <a class="nav-link disabled" href="#">Disabled</a>
            </li>
        </ul>
        {% if session.username %}
            <span class="username_loggedIn">Connecté sous {{ session.username }}</span>
            <form action="/?route=logOut" method="POST">
                <button class="btn btn-outline-success my-2 my-sm-0 ml-2" type="submit">Se déconnecter</button>
            </form>
        {% else %}
            <a href="/?route=signUp"><button class="btn btn-outline-success my-2 mr-2 my-sm-0">Créer un compte</button></a>
            <a href="/?route=logIn"><button class="btn btn-outline-success my-2 my-sm-0">Se connecter</button></a>
        {% endif %}
    </div>
</nav>

