{% extends 'base.html.twig' %}

{% block body %}

<table class="table table-striped table-bordered">
    <thead>
        <tr class="font-size-13">
            <th scope="col">Catégories</th>
            <th scope="col" class="center">Nombre de messages</th>
            <th scope="col" class="center">Date du dernier message</th>
            <th scope="col" class="center">Modérateurs</th>
        </tr>
    </thead>
    {% set i = 0 %}
    <tbody>
    {% for category in categories %}    
        <tr scope="row">
            <td><span class="font-weight-600">{{ category.name }}</span>
                <div>
                    {% for subclass in subclasses[i] %}
                        <span class="font-size-12">{{ subclass }}</span>
                    {% endfor %}
                </div>
            </td>
            <td class="center">{{ messageNumbers[i] }}</td>
            <td class="center">
                <span class="font-size-12">{{ datesAndAuthor[i]['publication_date'] }}</span>
                {% if datesAndAuthor[i]['username'] %}
                    <span class="font-size-12 font-weight-600 block">par {{ datesAndAuthor[i]['username'] }}</span>    
                {% endif %}
            </td>
            <td class="center">{% for moderator in moderators[i] %}
                <span class="font-size-12">{{ moderator }}</span>
                {% endfor %}
            </td>
        </tr>
        {% set i = i + 1 %}
    {% endfor %}
    </tbody>

</table>


{% endblock %}